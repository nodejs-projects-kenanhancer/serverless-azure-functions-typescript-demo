service: azure-functions-typescript

frameworkVersion: "2"

custom:
  webpack:
    webpackConfig: ./webpack.config.js
    includeModules: true

provider:
  name: azure
  runtime: nodejs12
  region: ${env:region, 'UK West'} #{UK West|UK South}
  stage: ${opt:stage, 'dev'}
  prefix: ${opt:prefix, 'kenan'}  # prefix of generated resource name
  # storageAccount:
  #   name: kenanhukwdevstorageaccount
  # resourceGroup: ${self:provider.prefix}-${self:provider.region}-${self:provider.stage}-${self:service}-rg # comment this field for default auto generated value.
  # deploymentName: ${self:provider.prefix}${self:provider.region}${self:provider.stage}${self:service}-DEPLOYMENT-t1619530107124 # comment this field for default auto generated value.
  # os: windows  # windows is default, linux is available
  # subscriptionId: 00000000-0000-0000-0000-000000000000
  # tenantId:

  environment: # these will be created as application settings
    VARIABLE_FOO: "HELLO WORLD"
    FIRST_NAME: "KENAN"
    LAST_NAME: "HANCER"

  # Default API Management configuration with Consumption sku(Stock-keeping-Unit)
  # apim: true

  # Start of your API Management configuration
  # apim:
  #   # name: ${self:provider.prefix}-${self:provider.region}-${self:provider.stage}-apim # comment this field for default auto generated value.
  #   sku:
  #     name: Developer #{Consumption|Developer|Basic|Standard|Premium}
  #     capacity: 1
  #   properties:
  #     publisherName: Kenan Hancer
  #     publisherEmail: kenanhancer@hotmail.com
  #   # API specifications
  #   apis:
  #       # Name of the API
  #     - name: greeting-api
  #       subscriptionRequired: false
  #       # Display name
  #       displayName: Greeting API
  #       # Description of API
  #       description: The Greeting REST API
  #       # HTTP protocols allowed
  #       protocols:
  #         - https
  #       # Base path of API calls
  #       path: products
  #       # Tags for ARM resource
  #       tags:
  #         - tag1
  #         - tag2
  #       # No authorization
  #       authorization: none
  #       # Name of the API
  #     - name: categories-api
  #       subscriptionRequired: false
  #       # Display name
  #       displayName: Categories API
  #       # Description of API
  #       description: The Categories REST API
  #       # HTTP protocols allowed
  #       protocols:
  #         - https
  #       # Base path of API calls
  #       path: categories
  #       # Tags for ARM resource
  #       tags:
  #         - tag1
  #         - tag2
  #       # No authorization
  #       authorization: none
  #   backends:
  #     - name: products-backend
  #       url: api/products
  #     - name: categories-backend
  #       url: api/categories
  #   # CORS Settings for APIM
  #   cors:
  #     allowCredentials: false
  #     allowedOrigins:
  #       - "*"
  #     allowedMethods:ß
  #       - GET
  #       - POST
  #       - PUT
  #       - DELETE
  #       - PATCH
  #     allowedHeaders:
  #       - "*"
  #     exposeHeaders:
  #       - "*"

  # you can define apim configuration here
  # apim:
  #   apis:
  #     - name: v1
  #       subscriptionRequired: false # if true must provide an api key
  #       displayName: v1
  #       description: V1 sample app APIs
  #       protocols:
  #         - https
  #       path: v1
  #       tags:
  #         - tag1
  #         - tag2
  #       authorization: none
  #   cors:
  #     allowCredentials: false
  #     allowedOrigins:
  #       - "*"
  #     allowedMethods:
  #       - GET
  #       - POST
  #       - PUT
  #       - DELETE
  #       - PATCH
  #     allowedHeaders:
  #       - "*"
  #     exposeHeaders:
  #       - "*"

plugins: # look for additional plugins in the community plugins repo: https://github.com/serverless/plugins
  - serverless-azure-functions
  - serverless-webpack
  - serverless-offline

functions:
  # When a http request comes to http://localhost:7071/api/hello, hello.sayHello function will be triggered and it will write log.
  hello:
    handler: src/handlers/http/hello.sayHello
    events:
      - http: true
        x-azure-settings:
          methods: 
            - get
          route: hello #<string>, default - <function name>
          authLevel: anonymous #<enum - anonymous|function (default)|admin>

  # When a http request comes to http://localhost:7071/api/bye, goodbye.sayGoodbye function will be triggered and it will write log.
  goodbye:
    handler: src/handlers/http/goodbye.sayGoodbye
    events:
      - http: true
        x-azure-settings:
          methods:
            - get
          route: bye
          authLevel: anonymous #<enum - anonymous|function (default)|admin>

  # When a http request comes to http://localhost:7071/api/addPerson, addperson.handle function will be triggered and it will add record to Person table.
  addPerson:
    handler: src/handlers/http/addperson.handle
    events:
      - http: true
        x-azure-settings:
          methods: 
            - get
            - post
          authLevel: anonymous #<enum - anonymous|function (default)|admin>
      - table: true
        x-azure-settings:
          name: personEntity
          tableName: Person
          direction: out
          connection: AzureWebJobsStorage # App Setting/environment variable which contains Storage Account Connection String

  # timer1.handle function will be triggered every 10 seconds and it will write log.
  timer1:
    handler: src/handlers/timer/timer1.printMessage
    events:
      - timer: true
        x-azure-settings:
          name: myTimer
          schedule: '*/10 * * * * *' #<string>, cron expression to run on

  # timer2.handle function will be triggered every 5 seconds and it will write log.
  timer2:
    handler: src/handlers/timer/timer2.handle
    events:
      - timer: true
        x-azure-settings:
          name: timerObj #<string>, default - "myTimer", specifies which name is available on `context.bindings`
          schedule: 0 */5 * * * * #<string>, cron expression to run on

  # When a file comes to samples-workitems/ folder, storageBlob1.handle function will be triggered and it will write log.
  storageBlob1:
    handler: src/handlers/storage/storageBlob1.handle
    events:
      - blob: true
        x-azure-settings:
          name: item # Specifies which name is available on `context`
          path: samples-workitems/{blobname}.{blobextension} # for more Blob name patterns infer to https://docs.microsoft.com/en-us/azure/azure-functions/functions-bindings-storage-blob-trigger?tabs=csharp
          connection: AzureWebJobsStorage # App Setting/environment variable which contains Storage Account Connection String
  
  # When a .yaml extension file comes to src/ folder, storageBlob2.handle function will be triggered and it will write log.
  StorageBlob2:
    handler: src/handlers/storage/storageBlob2.handle
    events:
      - blob: true
        x-azure-settings:
          name: item # Specifies which name is available on `context`
          path: src/{blobname}.yaml # for more Blob name patterns infer to https://docs.microsoft.com/en-us/azure/azure-functions/functions-bindings-storage-blob-trigger?tabs=csharp
          connection: AzureWebJobsStorage # App Setting/environment variable which contains Storage Account Connection String

  # When a .js extension file comes to src/ folder, storageBlob2.handle function will be triggered and it will write log.
  StorageBlob3:
    handler: src/handlers/storage/storageBlob2.handle
    events:
      - blob: true
        x-azure-settings:
          name: item # Specifies which name is available on `context`
          path: src/{blobname}.js # for more Blob name patterns infer to https://docs.microsoft.com/en-us/azure/azure-functions/functions-bindings-storage-blob-trigger?tabs=csharp
          connection: AzureWebJobsStorage # App Setting/environment variable which contains Storage Account Connection String

  # When a message comes to myqueue-items queue, storageQueue1.handle function will be triggered and it will write log.
  storageQueue1:
    handler: src/handlers/storage/storageQueue1.handle
    events:
      - queue: myqueue-items
        x-azure-settings:
          name: message # Specifies which name is available on `context`
          connection: AzureWebJobsStorage

  #  When a message comes to myqueue-items2 queue, storageQueue2.handle function will be triggered and it will retrieve file from samples-workitems2 blob and create a new copy in samples-workitems2 blob again
  storageQueue2:
    handler: src/handlers/storage/storageQueue2.handle
    events:
      - queue: myqueue-items2
        x-azure-settings:
          name: myQueueItem # Specifies which name is available on `context`
          connection: AzureWebJobsStorage
      - blob: true
        x-azure-settings:
          name: myInputBlob
          path: samples-workitems2/{queueTrigger}
          direction: in
          connection: AzureWebJobsStorage # App Setting/environment variable which contains Storage Account Connection String
      - blob: true
        x-azure-settings:
          name: myOutputBlob
          path: samples-workitems2/{queueTrigger}-Copy
          direction: out
          connection: AzureWebJobsStorage # App Setting/environment variable which contains Storage Account Connection String

  # When a message comes to myqueue-select-person-row2 queue, storageQueue3.handle function will be triggered and it will retrieve row from Person table.
  # partitionKey and rowKey are not specified so all records will come from Person table.
  storageQueue3:
    handler: src/handlers/storage/storageQueue3.handle
    events:
      - queue: myqueue-select-person-row2
        x-azure-settings:
          name: myQueueItem # Specifies which name is available on `context`
          connection: AzureWebJobsStorage
      - table: true
        x-azure-settings:
          name: personEntities
          tableName: Person
          direction: in
          connection: AzureWebJobsStorage # App Setting/environment variable which contains Storage Account Connection String

  # When a message comes to myqueue-insert-person-row queue, storageQueue4.handle function will be triggered and it will insert row in Person table.
  storageQueue4:
    handler: src/handlers/storage/storageQueue4.handle
    events:
      - queue: myqueue-insert-person-row
        x-azure-settings:
          name: myQueueItem # Specifies which name is available on `context`
          connection: AzureWebJobsStorage
      - table: true
        x-azure-settings:
          name: personEntity
          tableName: Person
          direction: out
          connection: AzureWebJobsStorage # App Setting/environment variable which contains Storage Account Connection String

  # When a message comes to myqueue-select-person-row queue, storageQueue5.handle function will be triggered and it will retrieve row from Person table.
  # partitionKey and rowKey are specified so only one record will come from Person table.
  storageQueue5:
    handler: src/handlers/storage/storageQueue5.handle
    events:
      - queue: myqueue-select-person-row
        x-azure-settings:
          name: myQueueItem # Specifies which name is available on `context`
          connection: AzureWebJobsStorage
      - table: true
        x-azure-settings:
          name: personEntity
          tableName: Person
          partitionKey: "{queueTrigger}"
          rowKey: "FirstName_{queueTrigger}"
          direction: in
          connection: AzureWebJobsStorage # App Setting/environment variable which contains Storage Account Connection String















  # The following are a few examples of other events you can configure:
  # storageBlob:
  #   handler: src/handlers/storageBlob.printMessage
  #   events:
  #     - blob:
  #       x-azure-settings:
  #         name: blob # Specifies which name is available on `context`
  #         path: blob-sample/{blobName}
  #         connection: AzureWebJobsStorage # App Setting/environment variable which contains Storage Account Connection String
  # storageQueue:
  #   handler: src/handlers/storageQueue.printMessage
  #   events:
  #     - queue: queue-sample
  #       x-azure-settings:
  #         name: message # Specifies which naem is available on `context`
  #         connection: AzureWebJobsStorage
  # timer:
  #   handler: src/handlers/timer.printMessage
  #   events:
  #     - timer:
  #       x-azure-settings:
  #         schedule: '*/10 * * * * *'
  # eventhub:
  #   handler: src/handlers/eventHub.printMessage
  #   events:
  #     - eventHub:
  #       x-azure-settings:
  #         name: eventHubMessages # Specifies which name it's available on `context`
  #         eventHubName: sample-hub # Specifies the Name of the Event Hub
  #         consumerGroup: $Default # Specifies the consumerGroup to listen with
  #         connection: EVENT_HUBS_CONNECTION # App Setting/environment variable which contains Event Hubs Namespace Connection String
  # serviceBusQueue:
  #   handler: src/handlers/serviceBusQueue.printMessage
  #   events:
  #     - serviceBus:
  #       x-azure-settings:
  #         name: message # Specifies which name is available on `context`
  #         queueName: sample-queue # Name of the service bus queue to consume
  #         connection: SERVICE_BUS_CONNECTION # App Setting/environment variable variable which contains Service Bus Namespace Connection String
  # serviceBusTopic:
  #   handler: src/handlers/serviceBusTopic.printMessage
  #   events:
  #     - serviceBus:
  #       x-azure-settings:
  #         name: message # Specifies which name it's available on `context`
  #         topicName: sample-topic # Name of the service bus topic to consume
  #         subscriptionName: sample-subscription # Name of the topic subscription to retrieve from
  #         connection: SERVICE_BUS_CONNECTION # App Setting/environment variable variable which contains Service Bus Namespace Connection String
